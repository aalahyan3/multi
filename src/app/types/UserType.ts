export type UserData = {
    username: string,
    email: string,
    first_name: string,
    last_name: string,
    specific_color: string,
    image_url: string | null,
    bio: string | null,
    last_seen: Date
  }
